import React from 'react';

const ErrorLayout = () => {
    return (
        <div>
            Error!!!
            Invalid link
            Please check the correctness of the web address
        </div>
    );
};

export default ErrorLayout;