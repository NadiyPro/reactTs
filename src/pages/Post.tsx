import React, {useState} from 'react';
import {useParams} from "react-router-dom";

const Post = () => {
    let {id} = useParams();


    return (
        <div>

        </div>
    );
};

export default Post;