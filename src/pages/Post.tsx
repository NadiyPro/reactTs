import React from 'react';
import {useParams} from "react-router-dom";

const Post = () => {
    const {id} = useParams()

    return (
        <div>
            ffff
        </div>
    );
};

export default Post;